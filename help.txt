Log In:
ssh triangle@triangle.local
Password: Those Who Know

EDIT STARTUP:
sudo nano /etc/rc.local

python3 -m pip install -U pygame==2.6.0

# TO RUN ON PI
python main.py

# TO RUN ON PC
python main.py -NOPI

### BEGIN SETUP
mkdir -p /home/triangle/.config/autostart
nano /home/triangle/.config/autostart/skeeball.desktop
    ### 
    [Desktop Entry]
    Type=Application
    Name=Skeeball
    Exec=lxterminal -e bash -c "cd /home/triangle/skeeball && python launcher.py -EOH; bash"
    ###
### END

can also 'sudo cp' the skeeball.desktop onto /home/triangle/desktop/FNAME.desktop
to have it clickable on main screen
